/**
 * The page's only decoration: a faint warm wash so the paper is not a flat fill.
 * Deliberately not animated and not blurred — a printed form has no aurora.
 */
export const PublicBackdrop = () => (
  <div className="vintage-paper-wash" aria-hidden="true" />
)
